---
title: Long-running work needs evidence
description: Checkpoints, failure visibility and proof distinguish durable execution from optimistic status.
courseId: models-to-agents
courseTitle: From models to Agents
courseOrder: 1
lessonId: long-running-work-needs-evidence
lessonOrder: 7
estimatedMinutes: 7
canonical:
  - label: Benchmark specification
    path: benchmarks/SPEC.md
  - label: AgentOS architecture
    path: ARCHITECTURE.md
---

## The mental model

As an outcome spans more tools and more time, the chance of interruption, stale assumptions and
partial success rises. Durable work needs checkpoints that another session can inspect and
completion evidence that an independent reviewer can verify.

Evidence is tied to the authority that owns the claim:

| Claim | Useful evidence |
| --- | --- |
| source changed | Git diff and commit |
| checks passed | exact command, exit status and useful output |
| review requested | provider review URL |
| workload recovered | identity, PVC, session and preserved-work observations |
| human approved | exact durable decision |

An ever-growing transcript is not a substitute. It may explain why the Agent acted, while native
artifacts prove what became true.

## Worked example: return a reviewable result

The onboarding Agent completes with:

```text
commit: 7f4c… add server-side address validation
checks: bun test onboarding (passed, 42 tests)
review: provider-native review artifact URL recorded in the delivery report
remaining risk: existing clients may rely on undocumented whitespace normalization
authority used: branch push + review creation; no merge
```

That report is short because the evidence is durable. If the test failed, preserving the failing
output and branch would be more truthful than marking the outcome complete.

## What changes at this layer?

- Work can survive and be assessed across time.
- Completion becomes a verifiable claim rather than a conversational feeling.
- Failures and unresolved risks remain visible.
- The next bottleneck appears: several durable Agents still need ownership and organization.

Continue to Course 2: [Parallel Agents are not yet a
company](/learn/02-agents-to-companies/parallel-agents-not-company).
