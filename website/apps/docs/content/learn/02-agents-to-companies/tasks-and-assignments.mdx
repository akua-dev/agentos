---
title: Tasks and Assignments
description: A Task preserves stable intent while an Assignment names the active accountable owner.
courseId: agents-to-companies
courseTitle: From Agents to companies
courseOrder: 2
lessonId: tasks-and-assignments
lessonOrder: 3
estimatedMinutes: 3
canonical:
  - label: Durable coordination
    path: database/migrations/0005_durable_coordination_contracts.sql
  - label: Atomic acceptance
    path: database/migrations/0011_atomic_task_acceptance.sql
---

## Intent and execution have different lifecycles

AgentOS separates two lifecycles:

- a **Task** is stable accepted intent;
- an **Assignment** is one bounded attempt owned by one accountable Agent.

Ownership can change without erasing history. There is one active accountable owner, not several
sessions with overlapping claims.

```text
Task: checkout rejects expired addresses
  ├─ Assignment 1: establish causal boundary → completed report
  └─ Assignment 2: implement and deliver → active
```

Atomic acceptance creates a Task and its first Assignment together. A handoff ends the current
Assignment and creates its successor through a released Function; direct row edits cannot
reconstruct that guarantee.

## Diagnose, then deliver

The checkout outcome first receives a scout Assignment. The scout reproduces the defect and proves
that a timezone conversion truncates the validity date. It changes no project source and returns a
report.

The responsible Mate then hands the Task to a ship Crewmate with the implementation boundary and
delivery workflow. The Task stays stable; the owner and bounded attempt change.

If the ship worker disappears, supervision recovers the same Assignment and worktree. It does not
create a second active owner merely to make progress visible.

Read the exact concept reference in [Tasks and Assignments](/docs/concepts/tasks-assignments).
Next, meet [a crew, not a swarm](/learn/02-agents-to-companies/crew-not-swarm).
