# First Mate runtime slice

Implement the approved persistent First Mate boundary without provider login,
database provisioning, image publication, or Crewmate spawning.

1. Specify cold and warm PVC-home preparation through an executable test, then
   implement locked Mise reconciliation, Pi trust, and the Herdr Pi integration.
2. Specify fresh start, native resume, duplicate refusal, and health behavior
   through a fake Herdr boundary, then implement the runtime scripts.
3. Specify the rendered Kubernetes contract, then add a small pinned image,
   namespaced StatefulSet base, and explicit cluster-admin overlay.
4. Run the focused tests, repository check, Kustomize renders, shell syntax, and
   a real local image build when Docker is available.
