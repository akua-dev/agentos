# Crewmate runtime test contract

Exercise the real Git worktree boundary and inspect complete process arguments.

- Prove Treehouse yields project work isolated from the Mate's primary checkout.
- Treat Herdr and harness executables as process boundaries, not source text.
- Never assert that implementation files merely contain arbitrary strings.
