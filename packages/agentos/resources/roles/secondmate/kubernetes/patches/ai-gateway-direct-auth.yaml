apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: agentos-secondmate
spec:
  template:
    spec:
      initContainers:
        - name: prepare-home
          env:
            - name: AGENTOS_PI_PROVIDER_MODE
              value: direct
