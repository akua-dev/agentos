{
  "name": "@agentos/fleet",
  "version": "0.0.0",
  "description": "Persistent AgentOS Fleet runtime and database workspace",
  "private": true,
  "type": "module",
  "workspaces": ["database"],
  "scripts": {
    "test": "bun test .",
    "typecheck": "tsc --project tsconfig.json",
    "check": "bun run test && bun run typecheck"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
