[tasks."firstmate:prepare"]
description = "Reconcile the persistent First Mate home"
file = "../../deploy/kubernetes/mate/runtime/prepare-home.ts"

[tasks."firstmate:run"]
description = "Run the persistent First Mate through Herdr and Pi"
file = "../../deploy/kubernetes/mate/runtime/run-mate.ts"

[tasks."firstmate:health"]
description = "Check First Mate liveness or readiness"
file = "../../deploy/kubernetes/mate/runtime/health.ts"

[tasks."mate:render"]
description = "Render one persistent Second Mate from explicit reviewed inputs"
file = "../../deploy/kubernetes/mate/render-secondmate.ts"
