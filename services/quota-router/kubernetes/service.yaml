apiVersion: v1
kind: Service
metadata:
  name: quota-router
  labels:
    app.kubernetes.io/name: quota-router
    app.kubernetes.io/part-of: agentos
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: quota-router
  ports:
    - name: http
      protocol: TCP
      port: 8787
      targetPort: http
